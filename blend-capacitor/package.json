{
  "name": "blend-capacitor",
  "version": "0.1.0",
  "private": true,
  "description": "Blend (Capacitor shell) \u2014 wraps www/ for iOS via Capacitor",
  "scripts": {
    "postinstall": "echo 'Installed. Next: npx cap add ios && npx cap sync && npx cap open ios'",
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/share": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  }
}